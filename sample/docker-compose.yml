version: "3.7"

services:
  sample:
    container_name: sample
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ".:/app"
      - "/app/node_modules"
    stdin_open: true
    tty: true
    ports:
      - 3001:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
